<h1> Books Index </h1>

<% @books.each do |book| %>
  <ol>
    <%= link_to book.title, book %>
    <%= book.author %>
    <%= book.publisher %>
    <%= book.genre %>
    <%= book.release_year %>
  </ol>
<% end %>